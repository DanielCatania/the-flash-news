import styled from "styled-components";

const QueuedList = styled.ul`
  /* posicioanmento */
  display: flex;
  align-items: center;
  justify-content: space-between;
  flex-wrap: wrap;
`;

export default QueuedList;
